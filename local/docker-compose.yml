services:
  postgres:
    image: postgres:16.4
    container_name: bancada_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: bancada
      POSTGRES_USER: ada.tech
      POSTGRES_PASSWORD: turma1660
    ports:
      - "5433:5433"
    volumes:
      - ./init:/docker-entrypoint-initdb.d:ro
      - bancada_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ada.tech -d bancada"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  bancada_data:

# ------------------------------------------------------------
# Como usar:
#   docker compose up -d
#
# Aguarde o container ficar healthy e então suba a aplicação:
#   ./mvnw quarkus:dev
#
# application.properties (Quarkus):
#   quarkus.datasource.db-kind=postgresql
#   quarkus.datasource.username=ada.tech
#   quarkus.datasource.password=turma1660
#   quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5433/bancada
#   quarkus.hibernate-orm.database.generation=none
# ------------------------------------------------------------
